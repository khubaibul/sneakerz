import React from 'react';
import Loading from '../../../Shared/Loading';

const Collection = () => {
    return (
        <div>
            <Loading />
        </div>
    );
};

export default Collection;